export const metadata = {
  title: 'Applications | GlobX',
  description: 'Manage your opportunity applications',
};

export default function ApplicationsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
